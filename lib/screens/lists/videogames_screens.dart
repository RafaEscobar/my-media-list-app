import 'package:flutter/material.dart';

class VideogamesScreens extends StatelessWidget {
  const VideogamesScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Text("Videojuegos"),
    );
  }
}